#include <bits/stdc++.h>
using namespace std;

int main() {
    char str[128] = "Hello World!";

    for (int i = 0; i < strlen(str); i++) {
        printf("%d, ", str[i]);
    }

    system("pause");
    return 0;
}