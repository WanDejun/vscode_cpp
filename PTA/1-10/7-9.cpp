#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main() {
    int a, b;
    scanf("%d%d%d", &a, &b, &c);

    if (a == b) printf("C");
    else if (b == c) printf("A");
    else printf("B");
    system("pause");
    return 0;
}